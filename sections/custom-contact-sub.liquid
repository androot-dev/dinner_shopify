{%- style -%}
.section-{{ section.id }}-padding {
  padding-top: calc({{ section.settings.padding_top }}px * 0.75);
  padding-bottom: calc({{ section.settings.padding_bottom }}px  * 0.75);
}
.section-{{ section.id }} {
  background-color: {{ section.settings.background-color }};
  height:auto;
}
@media screen and (min-width: 750px) {
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
}
{%- endstyle -%}

  <section class="contactUs my-section section-{{ section.id }}-padding section-{{ section.id }}">
    <div class="container contact-form">

        <h2 class=" pb-3 pt-5 text-center title-section">Contact Us</h2>

        <div class="row mb-5 align-items-center p-3">

            <div class="col-lg-6 ">
                <iframe class="mb-4 mb-lg-0" src="<?php the_field( 'map_embed_url' ); ?>"
                    style="border:0; width: 100%; height: 384px;" allowfullscreen="" loading="lazy"
                    referrerpolicy="no-referrer-when-downgrade"></iframe>
            </div>

            <div class="col-lg-6 form-col p-5 ">

                <div class="row ">
                    <h2 class="title text-center w-100"><?php the_field( 'title_form' ); ?></h2>
                </div>


                <?php echo do_shortcode( '[contact-form-7 id="417" title="contact us"]' ); ?>
            </div>

        </div>

        <div class="row mb-5">
            <?php if ( have_rows( 'more_information', 'option' ) ): ?>
            <?php while ( have_rows( 'more_information', 'option' ) ) : the_row(); ?>
            <?php if ( get_row_layout() == 'location' ) : ?>
            <div class="col-lg-6 ">



                <div class="info-box flex-column border-menu d-flex flex-wrap align-items-center mb-4">
                    <h3 class="d-flex align-items-center text-primary-normal justify-content-center"> <i
                            class="pr-2 bx <?php the_sub_field( 'icon' ); ?>"></i>Address
                    </h3>
                    <p class="m-0 p-0">
                        <a class="text-primary-normal color-secondary hover_a" target="_blank"
                            href=" <?php the_sub_field( 'link' ); ?>">
                            <?php the_sub_field( 'label' ); ?>
                        </a>
                    </p>
                </div>

            </div>
            <?php elseif ( get_row_layout() == 'Email' ) : ?>
            <div class="col-lg-3  col-md-6">
                <div class="info-box border-menu flex-column d-flex flex-wrap align-items-center mb-4">

                    <h3 class="d-flex align-items-center text-primary-normal"><i
                            class="pr-2 bx <?php the_sub_field( 'icon' ); ?>"></i>
                        Email Us
                    </h3>
                    <p class="mb-0 pb-0"> <a class="text-primary-normal color-secondary hover_a" target="_blank"
                            href="mailto:<?php the_sub_field( 'email' ); ?>">
                            <?php the_sub_field( 'label' ); ?>
                        </a> </p>
                </div>
            </div>
            <?php elseif ( get_row_layout() == 'phone' ) : ?>
            <div class="col-lg-3  col-md-6">
                <div class="info-box flex-column border-menu d-flex flex-wrap align-items-center mb-4">

                    <h3 class="d-flex align-items-center text-primary-normal"> <i
                            class="pr-2 bx <?php the_sub_field( 'icon' ); ?> "></i>Call Us
                    </h3>
                    <p class="mb-0 pb-0"><a class="text-primary-normal color-secondary hover_a" target="_blank"
                            href="tel:<?php the_sub_field( 'phone' ); ?>">
                            <?php the_sub_field( 'label' ); ?>
                        </a></p>
                </div>
            </div>
            <?php endif; ?>
            <?php endwhile; ?>
            <?php endif; ?>
        </div>


    </div>
  </section>




{% schema %}
{
  "name": "Custom Contact Section",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Custom Colors"
    },
    {
        "id": "background-color",
        "type": "color",
        "label": "Background Color"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 52
    }
  ],
  "presets": [
    {
      "name": "Custom Section"
    }
  ]
}
{% endschema %}
